const phone = new RegExp(
  `^(?:` +
    "1[3-9]\\d{9}$|" + // 中国大陆手机号
    "(?:852)?[69]\\d{7}$|" + // 香港手机号（带/不带区号）
    "(?:853)?6\\d{7}$|" + // 澳门手机号（带/不带区号）
    "(?:886)?9\\d{8}$" + // 台湾手机号（带/不带区号）
    ")$"
);

const code = /^\d{6}$/; //短信验证码

export default { phone, code };
